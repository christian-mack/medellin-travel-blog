import { Bebas_Neue, Inter } from "next/font/google";

export const bebas = Bebas_Neue({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});
